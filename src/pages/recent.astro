---
import RecentNotes from "../components/RecentNotes.astro";
import WorkspaceRouteRedirect from "../components/WorkspaceRouteRedirect.astro";
import { getWorkspaceSnapshot } from "../lib/vault-state";

const workspaceMode = getWorkspaceSnapshot().mode === "workspace";
---

{workspaceMode ? <WorkspaceRouteRedirect label="Recent notes" /> : <RecentNotes />}
