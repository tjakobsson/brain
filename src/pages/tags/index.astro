---
import TagsIndex from "../../components/TagsIndex.astro";

// In workspace mode this is the workspace-wide aggregate: every Brain's tags,
// each entry naming the Brains that use it. Brain-scoped tags live beneath
// `/brains/<id>/tags/`.
---

<TagsIndex />
